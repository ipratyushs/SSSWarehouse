const React = require("react-native");

const { StyleSheet } = React;

export default {
  container: {
    backgroundColor: "#fff"
  },
  titleText: {

    fontSize: 20,
    fontWeight: 'normal',
  }
};
